name: Build APK

on:
  push:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repo
        uses: actions/checkout@v4

      - name: 🟢 Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'yarn'

      # 1️⃣ Instalar dependências
      - name: 📦 Install deps
        run: yarn install --frozen-lockfile

      # 2️⃣ Prebuild do Expo (gera android/ios nativo)
      - name: ⚙️ Expo prebuild
        run: npx expo prebuild --platform android --non-interactive

      # 3️⃣ Gerar bundle JS embed (para usar no APK sem metro server)
      - name: 📦 Bundle JS embed
        run: npx expo export:embed --platform android --dev false --eager

      # 4️⃣ Dar permissão ao Gradle
      - name: 🔧 Give gradle permission
        run: chmod +x android/gradlew

      # 5️⃣ Build APK release rápido (Gradle compatível + plugin resolvido)
      - name: 🏗️ Build APK Release rápido
        run: |
          cd android
          # Força o uso de Gradle compatível (7.6 funciona bem com React Native/Expo)
          ./gradlew wrapper --gradle-version 7.6
          # Adiciona repositórios para plugins caso não estejam definidos
          echo "pluginManagement { repositories { gradlePluginPortal(); google(); mavenCentral() } }" >> settings.gradle.kts
          # Build rápido com flags de performance
          ./gradlew assembleRelease \
            --parallel \
            --configure-on-demand \
            --daemon \
            --offline \
            -Pandroid.injected.signing.store.file=./keystore.jks \
            -Pandroid.injected.signing.store.password=${{ secrets.KEYSTORE_PASSWORD }} \
            -Pandroid.injected.signing.key.alias=${{ secrets.KEY_ALIAS }} \
            -Pandroid.injected.signing.key.password=${{ secrets.KEY_PASSWORD }}

      # 6️⃣ Upload APK
      - name: 📤 Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: android/app/build/outputs/apk/release/*.apk
